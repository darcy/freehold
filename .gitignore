 /target/
orchestrator/target/
# a local `go build -o ../target/...` mishap can drop a binary directly under
# orchestrator/ — never commit it
orchestrator/freehold-agent-tools
**/.freehold/

# terraform state is sensitive — never committed
*.tfstate
*.tfstate.*
.terraform/

# node deps for the ai-pr-review workflow
node_modules/
